namespace drr.enrichment.common
version "6.34.1"

import drr.enrichment.lei.*
import drr.enrichment.upi.*
import drr.standards.iso.*

type EnrichmentData: <"Optionally populated by the pre-enrichment process.">
    micData MicData (0..*)
    leiData LeiData (0..*)
    upiData UpiData (0..*)
